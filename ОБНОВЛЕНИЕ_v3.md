# Обновление v3.0 - Только ручной выбор типа расписания

## 🎯 Что изменилось

### Убрано:
- ❌ **Автоматическое определение** типа расписания
- ❌ Кнопка **"🤖 Авто"**
- ❌ Функция `detectScheduleType()` с проверкой колонок
- ❌ Переменные `detectedScheduleType` и `manualOverride`
- ❌ Сложная логика определения по названиям колонок
- ❌ Сообщения "Обнаружено: расписание МСК/регионов"

### Добавлено:
- ✅ **Только ручной выбор** типа расписания
- ✅ Два варианта: **МСК** или **Регионы**
- ✅ Простой и понятный интерфейс
- ✅ Обязательный выбор перед обработкой
- ✅ Четкие сообщения о выбранном типе

## 🎨 Новый интерфейс

После загрузки файла расписания появляется панель:

```
┌─────────────────────────────────────────┐
│    ⚙️ Выберите тип расписания           │
│                                         │
│    [🏢 МСК]  [🌍 Регионы]              │
│                                         │
│  Выберите тип расписания перед обработкой│
└─────────────────────────────────────────┘
```

После выбора типа:

```
┌─────────────────────────────────────────┐
│    ⚙️ Выберите тип расписания           │
│                                         │
│    [🏢 МСК]  [  Регионы  ]             │
│    (активна)                            │
│                                         │
│  ✓ Выбрано: МСК (московский склад)     │
└─────────────────────────────────────────┘
```

## 🔄 Новый процесс работы

### Шаг 1: Загрузите файлы
1. Отчёт по ОС на отгрузку
2. Расписание
3. (Опционально) POSM

### Шаг 2: Выберите тип расписания
**Появится панель с двумя кнопками:**
- **🏢 МСК** - для московского склада
- **🌍 Регионы** - для региональных складов

**Нажмите нужную кнопку!**

### Шаг 3: Обработайте данные
Нажмите **"Обработать данные"**

### ⚠️ Важно!
Если не выбрать тип расписания, появится ошибка:
```
❌ Выберите тип расписания (МСК или Регионы)
```

## 💬 Новые сообщения

### При загрузке расписания:
```
ℹ️ Выберите тип расписания: МСК или Регионы
```

### При выборе МСК:
```
✓ Тип расписания: МСК
```

### При выборе Регионы:
```
✓ Тип расписания: Регионы
```

### Попытка обработки без выбора:
```
❌ Выберите тип расписания (МСК или Регионы)
```

### После сброса:
```
ℹ️ Все данные сброшены
```

## 📊 Информационная панель

Под кнопками всегда отображается текущий статус:

| Состояние | Сообщение |
|-----------|-----------|
| Не выбрано | `Выберите тип расписания перед обработкой` |
| Выбрано МСК | `✓ Выбрано: МСК (московский склад)` |
| Выбрано Регионы | `✓ Выбрано: Регионы` |

## 🎯 Когда выбирать какой тип?

### Выбирайте **МСК** если:
- Расписание для московского склада
- В файле колонки на английском: `CITY`, `SC`, `BRAND`, `STORE N`, `ORDER DAY`, `DISPATCH DAY`
- Нужна логика с последовательностью брендов: VILET=1, ECRU=2, DUB=3, MAAG=4
- Группировка по паллетам и местам

### Выбирайте **Регионы** если:
- Расписание для региональных складов
- В файле колонки на русском: `ТЦ`, `Временная отмена`, `Последов-ть загрузки ТЦ`
- Нужно обогащение данными из расписания (ТЦ, последовательности)
- Нормализация брендов и добавление POSM строк

## 🔧 Технические изменения

### Удалено из кода:

**HTML:**
```html
<!-- Удалена кнопка Авто -->
<button class="type-btn auto active" id="typeAuto" onclick="setScheduleType('auto')">🤖 Авто</button>
```

**CSS:**
```css
/* Удалены стили для .auto */
.schedule-type-selector .type-btn.auto { ... }
.schedule-type-selector .type-btn.auto.active { ... }
```

**JavaScript:**
```javascript
// Удалены переменные
let detectedScheduleType = null;
let manualOverride = false;

// Упрощена функция detectScheduleType
// Теперь только показывает селектор без определения

// Упрощена функция setScheduleType
// Убрана проверка на 'auto'

// Упрощена updateScheduleTypeInfo
// Убраны сообщения об автоопределении

// Упрощена updateScheduleTypeButtons
// Убрана работа с кнопкой Авто

// Упрощена resetAll
// Убран сброс переменных detectedScheduleType и manualOverride
```

### Новый код функций:

#### detectScheduleType():
```javascript
function detectScheduleType(scheduleData) {
    // Simply show the selector - user must choose manually
    document.getElementById('scheduleTypeSelector').classList.remove('hidden');
    showMessage('Выберите тип расписания: МСК или Регионы', 'info');
}
```

#### setScheduleType():
```javascript
function setScheduleType(type) {
    scheduleType = type;
    showMessage(`Тип расписания: ${type === 'MSC' ? 'МСК' : 'Регионы'}`, 'success');
    
    updateScheduleTypeInfo();
    updateScheduleTypeButtons();
}
```

#### updateScheduleTypeInfo():
```javascript
function updateScheduleTypeInfo() {
    const infoDiv = document.getElementById('detectedInfo');
    
    if (scheduleType === 'MSC') {
        infoDiv.textContent = '✓ Выбрано: МСК (московский склад)';
    } else if (scheduleType === 'REG') {
        infoDiv.textContent = '✓ Выбрано: Регионы';
    } else {
        infoDiv.textContent = 'Выберите тип расписания перед обработкой';
    }
}
```

#### updateScheduleTypeButtons():
```javascript
function updateScheduleTypeButtons() {
    document.getElementById('typeMSC').classList.remove('active');
    document.getElementById('typeREG').classList.remove('active');

    if (scheduleType === 'MSC') {
        document.getElementById('typeMSC').classList.add('active');
    } else if (scheduleType === 'REG') {
        document.getElementById('typeREG').classList.add('active');
    }
}
```

## 🎉 Преимущества новой версии

### 1. Простота
- ✅ Нет сложной логики определения
- ✅ Пользователь сам знает какой тип нужен
- ✅ Меньше кода, меньше возможностей для ошибок

### 2. Надежность
- ✅ Не зависит от названий колонок
- ✅ Нет ложных определений
- ✅ Пользователь всегда контролирует процесс

### 3. Понятность
- ✅ Четкие сообщения
- ✅ Два варианта вместо трех
- ✅ Визуально видно что выбрано

### 4. Безопасность
- ✅ Обязательный выбор перед обработкой
- ✅ Нельзя обработать с неопределенным типом
- ✅ Всегда понятно какая логика используется

## 📝 Примеры использования

### Пример 1: Обработка расписания МСК
```
1. Загружаю "Отчёт по ОС на отгрузку"           ✓
2. Загружаю расписание МСК                      ✓
3. Появляется панель выбора
4. Нажимаю "🏢 МСК"                             ✓
5. Вижу: "✓ Выбрано: МСК (московский склад)"
6. Нажимаю "Обработать данные"                  ✓
7. Получаю результат с логикой MSC              ✓
```

### Пример 2: Обработка расписания регионов
```
1. Загружаю "Отчёт по ОС на отгрузку"           ✓
2. Загружаю расписание регионов                 ✓
3. Появляется панель выбора
4. Нажимаю "🌍 Регионы"                         ✓
5. Вижу: "✓ Выбрано: Регионы"
6. Загружаю POSM (опционально)                  ✓
7. Нажимаю "Обработать данные"                  ✓
8. Получаю результат с логикой REG              ✓
```

### Пример 3: Попытка обработки без выбора
```
1. Загружаю "Отчёт по ОС на отгрузку"           ✓
2. Загружаю расписание                          ✓
3. Появляется панель выбора
4. НЕ нажимаю ни одну кнопку
5. Нажимаю "Обработать данные"                  ✗
6. Вижу ошибку: "❌ Выберите тип расписания"
7. Возвращаюсь к шагу 4 и выбираю тип          ✓
```

### Пример 4: Изменение выбора
```
1. Загружаю файлы                               ✓
2. Нажимаю "🏢 МСК"                             ✓
3. Передумал - нажимаю "🌍 Регионы"            ✓
4. Кнопка МСК становится неактивной
5. Кнопка Регионы становится активной
6. Обрабатываю с логикой REG                   ✓
```

## 🔄 Сравнение с предыдущей версией

| Параметр | v2.0 (Авто + Ручной) | v3.0 (Только ручной) |
|----------|----------------------|----------------------|
| Кнопок выбора | 3 (Авто, МСК, Регионы) | 2 (МСК, Регионы) |
| По умолчанию | Авто (активна) | Ничего не выбрано |
| Определение типа | Автоматическое по колонкам | Ручной выбор |
| Сообщения | Сложные, длинные | Простые, короткие |
| Строк кода | ~1485 | ~1410 (-75 строк) |
| Переменных | 7 | 5 (-2) |
| Логика | Сложная | Простая |
| Надежность | Зависит от названий колонок | Не зависит |

## ✅ Что осталось без изменений

- ✅ Весь внешний вид MSC
- ✅ Логика обработки MSC
- ✅ Логика обработки REG
- ✅ Фильтрация данных
- ✅ Группировка и подсчеты
- ✅ POSM интеграция
- ✅ Статистика
- ✅ Предварительный просмотр
- ✅ Экспорт в Excel
- ✅ Печать
- ✅ Drag & Drop
- ✅ Адаптивный дизайн

## 📦 Итог

**Версия 3.0** проще, надежнее и понятнее чем версия 2.0:

- **Меньше кода** → меньше ошибок
- **Проще интерфейс** → легче использовать  
- **Ручной выбор** → больше контроля
- **Нет автоопределения** → нет ложных срабатываний

**Версия:** 3.0  
**Дата:** 2024-11-05  
**Статус:** ✅ Готово к использованию
