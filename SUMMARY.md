# Main_Merged.html - Полная документация

## 📦 Что это?

Универсальный HTML-файл для приоритизации загрузки транспорта, который автоматически определяет тип расписания (МСК или Регионы) и применяет соответствующую логику обработки.

## ✨ Ключевые возможности

### 1. Автоматическое определение типа расписания
- **МСК**: по колонкам `CITY`, `SC`, `BRAND`, `STORE N`, `ORDER DAY`, `DISPATCH DAY`
- **Регионы**: по колонкам `ТЦ`, `Временная отмена`, `Последов-ть загрузки`

### 2. Ручной выбор типа расписания
Три режима работы:
- 🤖 **Авто** - автоматическое определение (по умолчанию)
- 🏢 **МСК** - принудительное использование логики московского склада
- 🌍 **Регионы** - принудительное использование логики региональных складов

### 3. Универсальный интерфейс
- Внешний вид из версии MSC (градиенты, анимации, modern design)
- Drag & Drop загрузка файлов
- Адаптивный дизайн для мобильных устройств
- Оптимизированная печать

### 4. Полная функциональность
- Фильтрация данных (исключение MOP, POSM)
- Группировка по паллетам/местам
- Подсчёт уникальных коробов
- Статистика обработки
- Предварительный просмотр (100 записей)
- Экспорт в Excel
- Печать таблицы

## 🎨 Внешний вид

### Цветовая схема MSC:
- **Основной**: `#00AEEF` (голубой)
- **Акцент**: `#A7C796` (зелёный)
- **Вторичный**: `#425563` (серый)
- **Светло-голубой**: `#B8DFEC`

### Элементы интерфейса:
- Градиентный заголовок (голубой → зелёный)
- Карточки загрузки файлов с эффектами hover
- Кнопки с тенями и анимациями
- Таблица с чередующимися строками
- Плавные переходы и трансформации

## 📊 Структура данных

### Входные файлы:

#### Файл 1: Отчёт по ОС на отгрузку (обязательно)
```
Номер заказа | Номер магазина | Направление | Номер рейса | Место | Паллет | Короб | Дата отгрузки | Тип заказа
```

#### Файл 2а: Расписание МСК (обязательно)
```
CITY | SC | BRAND | STORE N | ORDER DAY | DISPATCH DAY | CUT OFF TIME FOR ORDER | DISPATCH TIME
```

#### Файл 2б: Расписание регионов (обязательно)
```
Бренд | Номер магазина | ТЦ | Последов-ть загрузки ТЦ | Последов-ть загрузки от кабины | Временная отмена
```

#### Файл 3: POSM (опционально)
```
Рейс | магазин | Бренд | Наименование | Размер | Вес | Кол-во | Последовательность
```

### Выходные данные:

```
Транспорт/№ заказа | № магазина | Бренд/ТЦ | Место | Кол-во кор | Последов-ть Бренда в ТС | Последов-ть внутри Бренда | Коммент.
```

## 🔄 Логика обработки

### MSC Logic:
1. Группировка по паллетам и местам
2. Подсчёт уникальных коробов на каждом паллете
3. Определение последовательности брендов:
   - VILET = 1
   - ECRU = 2
   - DUB = 3
   - MAAG = 4
4. Внутренняя последовательность:
   - Текстиль = 1 (по умолчанию)
   - Трансфер = 2
   - POSM = 3
   - Оборудование = 4
5. Добавление данных из POSM файла
6. Сортировка: Транспорт → Бренд → Внутренняя последовательность
7. Перенумерация брендов внутри каждого транспорта

### REG Logic:
1. Группировка по ключу: транспорт + магазин + бренд + место
2. Подсчёт уникальных коробов
3. Обогащение данными из расписания:
   - ТЦ (торговый центр)
   - Последовательность загрузки ТЦ
   - Последовательность загрузки от кабины
4. Нормализация брендов:
   - МААГ / МАAG → MAAG
   - ЭКРЮ → ECRU
   - ДАБ / DAB → DUB
   - ВИЛЕТ → VILET
5. Добавление POSM строк после соответствующих брендов
6. Сортировка: Последов-ть Бренда в ТС → Последов-ть внутри Бренда → № магазина → Коммент.

### Общая фильтрация (для обоих типов):
- ❌ Исключаются заказы типа "MOP"
- ❌ Исключаются заказы типа "POSM"
- ❌ Исключаются магазины с буквами в номере

## 🎯 Использование

### Базовый сценарий:
```
1. Откройте Main_Merged.html в браузере
2. Загрузите "Отчёт по ОС на отгрузку"
3. Загрузите "Расписание" (МСК или Регионы)
4. (Опционально) Загрузите "POSM"
5. Система покажет определённый тип расписания
6. Нажмите "Обработать данные"
7. Просмотрите результаты
8. Скачайте Excel или распечатайте
```

### С ручным выбором:
```
1-3. Загрузите файлы (как выше)
4. Появится селектор типа расписания
5. Если определение неверное - выберите тип вручную:
   - Нажмите "МСК" для московского склада
   - Нажмите "Регионы" для региональных складов
6. Нажмите "Обработать данные"
7. Просмотрите и экспортируйте результаты
```

## 🔧 Технические детали

### Технологии:
- HTML5
- CSS3 (Flexbox, Grid, Animations)
- Vanilla JavaScript (ES6+)
- XLSX.js 0.18.5 (CDN)

### Браузеры:
- ✅ Chrome 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Edge 90+
- ✅ Opera 76+

### Особенности:
- Полностью клиентская обработка (без сервера)
- Работает offline после первой загрузки
- Обработка файлов до 50MB
- Поддержка кириллицы
- UTF-8 encoding

### Переменные состояния:
```javascript
let file1Data = null;           // Отчёт по ОС
let file2Data = null;           // Расписание
let file3Data = null;           // POSM (опционально)
let processedData = null;       // Обработанные данные
let scheduleType = null;        // Активный тип ('MSC' или 'REG')
let detectedScheduleType = null; // Автоматически определённый тип
let manualOverride = false;     // Флаг ручного выбора
```

### Ключевые функции:

#### Определение типа:
- `detectScheduleType(scheduleData)` - автоматическое определение
- `setScheduleType(type)` - ручная установка типа
- `updateScheduleTypeInfo()` - обновление информации
- `updateScheduleTypeButtons()` - обновление кнопок

#### Обработка данных:
- `processData()` - главная функция обработки
- `processDataMSC(filteredData)` - логика MSC
- `processDataREG(filteredData)` - логика REG
- `filterFile1Data(data)` - фильтрация отчёта

#### Утилиты:
- `normalizeColumnName(name)` - нормализация имён колонок
- `getColumnValue(row, ...names)` - получение значения колонки
- `normalizeBrand(brandName)` - нормализация названий брендов

#### UI:
- `displayPreview(data)` - отображение таблицы
- `downloadExcel()` - экспорт в Excel
- `printTable()` - печать
- `resetAll()` - сброс всех данных

## 📈 Статистика

После обработки отображается:
- **Всего записей** - количество строк в исходном файле
- **После фильтрации** - количество после удаления MOP/POSM
- **Транспортных средств** - уникальные номера рейсов
- **Брендов** - уникальные бренды

## 📥 Экспорт

### Excel файл:
- Формат: `.xlsx`
- Имя: `Приоритизация_загрузки_YYYY-MM-DDTHH-MM-SS.xlsx`
- Ширина колонок: автоматическая
- Кодировка: UTF-8
- Все обработанные данные (не только первые 100)

### Печать:
- Ориентация: альбомная
- Поля: 1см
- Скрываются: header, кнопки, статистика, селектор
- Отображается: заголовок, дата, таблица
- Разрыв страниц: избегается внутри строк

## 🛠️ Разработка

### Структура файла:
```
Main_Merged.html
├── <head>
│   ├── Meta tags
│   ├── Title
│   ├── XLSX.js CDN
│   └── <style> (CSS)
├── <body>
│   └── <div class="container">
│       ├── <header>
│       └── <div class="content">
│           ├── File upload section
│           ├── Schedule type selector  ← НОВОЕ
│           ├── Buttons section
│           ├── Messages
│           ├── Stats section
│           └── Preview section
└── <script> (JavaScript)
    ├── Variables
    ├── Constants (brands, mapping)
    ├── Helper functions
    ├── Type detection  ← УЛУЧШЕНО
    ├── MSC logic
    ├── REG logic
    ├── UI functions
    └── Event listeners
```

### Добавленные в v2.0:

#### CSS (новые классы):
- `.schedule-type-selector` - контейнер селектора
- `.type-buttons` - контейнер кнопок
- `.type-btn` - стиль кнопки
- `.type-btn.active` - активная кнопка
- `.type-btn.auto` - кнопка Авто
- `.detected-info` - информационное сообщение

#### HTML (новые элементы):
- `<div id="scheduleTypeSelector">` - селектор типа
- `<button id="typeAuto">` - кнопка Авто
- `<button id="typeMSC">` - кнопка МСК
- `<button id="typeREG">` - кнопка Регионы
- `<div id="detectedInfo">` - информация о типе

#### JavaScript (новые функции):
- `setScheduleType(type)` - установка типа
- `updateScheduleTypeInfo()` - обновление информации
- `updateScheduleTypeButtons()` - обновление кнопок

#### JavaScript (улучшенные функции):
- `detectScheduleType()` - теперь ищет английские колонки MSC
- `resetAll()` - теперь скрывает и сбрасывает селектор

## 🐛 Устранение неполадок

### Проблема: Селектор не появляется
**Решение:**
- Убедитесь, что файл расписания загружен
- Проверьте, что файл содержит данные
- Откройте консоль браузера (F12) для проверки ошибок

### Проблема: Неверное определение типа
**Решение:**
- Используйте ручной выбор типа
- Проверьте названия колонок в Excel файле
- Убедитесь, что файл соответствует ожидаемой структуре

### Проблема: Ошибка при обработке
**Решение:**
- Проверьте, что все обязательные колонки присутствуют
- Убедитесь, что данные корректны (числа в числовых полях)
- Попробуйте другой тип расписания вручную

### Проблема: Пустой результат
**Решение:**
- Проверьте, что данные не отфильтрованы (нет только MOP/POSM)
- Убедитесь, что номера магазинов числовые
- Попробуйте обработать без POSM файла

## 📚 Дополнительные документы

- `README_MERGED.md` - техническая документация
- `ОПИСАНИЕ.md` - подробное описание на русском
- `ИЗМЕНЕНИЯ.md` - журнал изменений v2.0
- `ИНСТРУКЦИЯ.md` - детальная инструкция пользователя

## 🎉 Готово к использованию!

Файл **Main_Merged.html** полностью автономен и готов к работе. Просто откройте его в браузере и начните загружать файлы!

**Версия:** 2.0  
**Дата:** 2024-11-05  
**Статус:** ✅ Готов к продакшн
