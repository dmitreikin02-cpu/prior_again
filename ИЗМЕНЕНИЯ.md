# Обновления Main_Merged.html

## Версия 2.0 - Ручной селектор типа расписания

### Что добавлено:

#### 1. Улучшенное автоматическое определение типа расписания

**Расписание МСК** теперь правильно определяется по колонкам:
- `CITY` - город
- `SC` - склад
- `BRAND` - бренд
- `STORE N` - номер магазина
- `ORDER DAY` - день заказа
- `DISPATCH DAY` - день отгрузки
- `CUT OFF TIME FOR ORDER` - время закрытия заказа
- `DISPATCH TIME` - время отгрузки

**Расписание регионов** определяется по колонкам:
- `ТЦ` - торговый центр
- `Временная отмена`
- `Последов-ть загрузки ТЦ`
- `Последов-ть загрузки от кабины`

#### 2. Ручной селектор типа расписания

Добавлен визуальный селектор с тремя кнопками:
- **🤖 Авто** - автоматическое определение (по умолчанию)
- **🏢 МСК** - принудительное использование логики московского склада
- **🌍 Регионы** - принудительное использование логики региональных складов

#### 3. Информационная панель

Под селектором отображается информация:
- Какой тип расписания был обнаружен автоматически
- Какой тип используется в данный момент
- Была ли произведена ручная настройка

### Примеры сообщений:

При автоматическом определении:
```
🔍 Обнаружено: расписание МСК (ORDER DAY, DISPATCH DAY)
```

При ручном выборе:
```
🔍 Обнаружено: расписание регионов (ТЦ, Последовательность) | ✋ Выбрано вручную
```

### Как использовать:

1. **Загрузите файлы** как обычно
2. **При загрузке расписания** появится селектор типа
3. **Автоматический режим** (по умолчанию):
   - Система сама определит тип по колонкам
   - Кнопка "Авто" будет активна (зелёная)
4. **Ручной режим**:
   - Нажмите кнопку "МСК" или "Регионы"
   - Выбранная кнопка станет активной (синяя)
   - Логика будет использовать выбранный тип независимо от содержимого файла

### Когда использовать ручной режим:

- Если автоматическое определение работает неправильно
- Если структура файла нестандартная
- Для тестирования разных логик на одних данных
- Если в расписании отсутствуют ключевые колонки

### Технические детали:

**Новые переменные:**
```javascript
let detectedScheduleType = null;  // Автоматически определённый тип
let manualOverride = false;        // Флаг ручного выбора
```

**Новые функции:**
- `setScheduleType(type)` - установка типа вручную
- `updateScheduleTypeInfo()` - обновление информационного сообщения
- `updateScheduleTypeButtons()` - обновление активной кнопки

**Улучшенная логика определения:**
- Проверка нескольких паттернов для MSC
- Более строгая проверка комбинаций колонок
- Поддержка как английских, так и русских названий

### Стилизация:

Селектор использует цветовую схему MSC:
- Основной цвет: `#00AEEF` (голубой)
- Акцентный цвет: `#A7C796` (зелёный)
- Белый фон с рамкой
- Плавные анимации при наведении
- Адаптивный дизайн для мобильных устройств

### Сброс данных:

При нажатии кнопки "🔄 Сбросить":
- Селектор скрывается
- Тип сбрасывается на "Авто"
- Все переменные обнуляются
- Кнопка "Авто" становится активной

### Совместимость:

- ✅ Работает со всеми браузерами (Chrome, Firefox, Safari, Edge, Opera)
- ✅ Адаптируется под размер экрана
- ✅ Скрывается при печати
- ✅ Сохраняет все функции предыдущей версии

### Пример использования:

#### Сценарий 1: Автоматическое определение работает
```
1. Загружаете расписание МСК
2. Система показывает: "🔍 Обнаружено: расписание МСК"
3. Оставляете "Авто" активным
4. Обрабатываете данные - используется логика MSC
```

#### Сценарий 2: Нужна ручная настройка
```
1. Загружаете расписание, но определение неверное
2. Видите сообщение: "🔍 Обнаружено: расписание регионов"
3. Знаете, что это МСК - нажимаете кнопку "МСК"
4. Сообщение меняется: "... | ✋ Выбрано вручную"
5. Обрабатываете данные - используется логика MSC
```

#### Сценарий 3: Переключение между типами
```
1. Выбрали "МСК" вручную
2. Обработали данные
3. Хотите попробовать логику регионов
4. Нажимаете "Регионы"
5. Обрабатываете заново - используется логика REG
```

### Что осталось без изменений:

- ✅ Весь внешний вид интерфейса MSC
- ✅ Логика обработки данных MSC и REG
- ✅ Функции экспорта и печати
- ✅ Статистика и предварительный просмотр
- ✅ Drag & Drop загрузка файлов
